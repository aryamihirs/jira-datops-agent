# VERCEL QUICK DEMO - MINIMAL DEPENDENCIES
# FastAPI and ASGI server
fastapi==0.115.5
uvicorn==0.32.1

# Database
sqlalchemy==2.0.36

# JIRA Integration
jira==3.8.0

# Slack Integration (lightweight)
slack-sdk==3.33.4

# AI - Lightweight only
google-genai>=0.1.0

# Configuration
python-dotenv==1.0.1
pydantic==2.10.2
pydantic-settings==2.6.1

# HTTP Client
httpx==0.27.2
requests==2.32.3

# Monitoring
loguru==0.7.3

# Multipart support
python-multipart>=0.0.9

# REMOVED FOR VERCEL DEMO (under 250MB limit):
# - chromadb, llama-index (RAG disabled for demo)
# - presidio (PII detection disabled)
# - pandas, numpy (not needed for demo)
# - celery, redis (no background jobs)
# - alembic, pytest (not needed in production)
# - email/parsing libs (can add back if needed)
