import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Backend is now deployed separately
  // API calls are configured via NEXT_PUBLIC_API_URL environment variable
};

export default nextConfig;
